1. The hottest day was 134 degrees, and it was in zip code 94063.




2.

SELECT
	start_station,
	COUNT(trip_id) as Number_of_trips_started
FROM
	trips
Group by 1;






3.

SELECT 
	trip_id,
	MIN(duration) as trip_length
FROM 
	trips
GROUP by 1
ORDER BY trip_length ASC;

trip id # 1116337 is the shortest trip

